{
  "name": "onDataZoom",
  "description": "Fired when the data zoom range changes (inside zoom or slider).",
  "schema": {
    "type": "object",
    "properties": {
      "componentType": {
        "type": "string",
        "description": "ECharts component type emitting the event (e.g. 'dataZoom')."
      },
      "seriesType": {
        "type": "string",
        "description": "Series type, when applicable."
      },
      "seriesIndex": {
        "type": "integer",
        "description": "Index of the series, when applicable."
      },
      "seriesName": {
        "type": "string",
        "description": "Series name, when applicable."
      },
      "name": {
        "type": "string",
        "description": "Associated name, when applicable."
      },
      "dataIndex": {
        "type": "integer",
        "description": "Index of the data item, when applicable."
      },
      "value": {
        "description": "Associated value, when applicable."
      }
    }
  }
}
